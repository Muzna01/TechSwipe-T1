import { Inter, Lusitana, Rubik } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const paragraph = Lusitana({ subsets: ['latin'], weight: '400' });
export const heading = Rubik({ subsets: ['arabic'] });